# Example env file for local development (DO NOT commit real secrets)
# Copy this to `backend/env.list` locally and fill real values.
# Use a secure random JWT secret in production and set it as a secret in your PaaS.

PORT=8090
SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5432/ecomarket_dev
SPRING_DATASOURCE_USERNAME=devuser
SPRING_DATASOURCE_PASSWORD=devpass
SPRING_PROFILES_ACTIVE=dev,db
SPRING_JPA_HIBERNATE_DDL_AUTO=update
JWT_SECRET=REPLACE_WITH_A_STRONG_RANDOM_SECRET
FILE_UPLOAD_DIR=/tmp/uploads
# Ejemplo de archivo de variables de entorno para ejecutar el backend
# Copia este archivo a `env.list` y edítalo antes de usarlo, o pasa las variables en la UI de Render.

# Puerto que tu aplicación expondrá *dentro* del contenedor.
# El proyecto usa `server.port=8090` en el perfil `dev` (local), por eso usamos 8090 aquí.
# En Render la plataforma provee la variable `PORT` automáticamente; no pongas "$PORT" literal en la UI.
PORT=8090

# Conexión a la base de datos (reemplaza los valores por los reales)
# - Para pruebas locales desde Docker Desktop: usa host.docker.internal y el puerto local de Postgres
# - Para Render DB: reemplaza por la URL que Render te provea
SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5440/ecomarket
SPRING_DATASOURCE_USERNAME=ecomarket_user
SPRING_DATASOURCE_PASSWORD=TuPassSegura123

# Perfil activo (envs separados por coma). En producción usa `prod` y configura ddl-auto=validate
SPRING_PROFILES_ACTIVE=dev,db
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# JWT secret (reemplaza por un secreto fuerte en producción)
JWT_SECRET=CHANGE_ME_GENERATE_A_LONG_RANDOM_SECRET

# Directorio de uploads dentro del contenedor (si necesitas persistencia, usa S3)
FILE_UPLOAD_DIR=/tmp/uploads
